# ICF catalogue (stroke gait core-set subset)

ICF catalogue (stroke gait core-set subset)

## Usage

``` r
icf_catalog()
```

## Value

A data.frame of ICF codes: \`code\`, \`component\`, \`title_en\`,
\`title_ja\`.
