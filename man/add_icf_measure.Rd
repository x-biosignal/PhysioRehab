% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/icf-construct.R
\name{add_icf_measure}
\alias{add_icf_measure}
\title{Add a measure observation to an ICF construct}
\usage{
add_icf_measure(
  construct,
  source,
  value,
  qualifier_type = c("capacity", "performance"),
  modality = c("scale", "rasch", "performance_test", "sensor", "self_report"),
  date = NA,
  unit = NA_character_,
  direction = c("higher_better", "higher_worse"),
  qualifier = NULL,
  cutpoints = NULL
)
}
\arguments{
\item{construct}{an `icf_construct`.}

\item{source}{short measure id, e.g. `"barthel"`, `"steps_per_day"`.}

\item{value}{numeric measure value(s).}

\item{qualifier_type}{`"capacity"` or `"performance"`.}

\item{modality}{`"scale"`, `"rasch"`, `"performance_test"`, `"sensor"` or
`"self_report"`.}

\item{date}{measurement date(s) (Date or coercible); recycled to `value`.}

\item{unit}{optional measure unit.}

\item{direction}{`"higher_better"` (default) or `"higher_worse"`.}

\item{qualifier}{optional explicit ICF qualifier(s) 0-4 (overrides
`cutpoints`).}

\item{cutpoints}{optional length-4 ascending cut-points mapping `value` to the
0-4 qualifier.}
}
\value{
the updated `icf_construct`.
}
\description{
Records one modality's view of the domain, tagged as `capacity` (a
standardised/clinic measure -- an ordinal scale total, a Rasch person measure,
a performance test) or `performance` (a free-living / real-world measure). The
value is mapped to the ICF 0-4 qualifier either directly (`qualifier`) or from
`cutpoints` (direction-aware), so heterogeneous measures become comparable.
`value` and `date` may be vectors to add a whole longitudinal stream at once.
}
\seealso{
[capacity_performance_gap()], [icf_construct_trajectory()]
}
